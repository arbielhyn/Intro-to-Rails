<!DOCTYPE html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Project</title>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <style>
    /* Optional: Additional custom styles */
    .form-control {
      padding: 0.5rem;
      border: 1px solid #ccc;
      border-radius: 0.25rem;
    }
  </style>
</head>
<body class="bg-gray-100">
  <header class="bg-blue-500 text-white py-4">
    <nav class="flex justify-center">
      <ul class="flex space-x-4 list-none">
        <li><%= link_to 'Recreation Complexes', recreation_complexes_path, class: "hover:text-gray-200" %></li>
        <li><%= link_to 'Libraries', libraries_path, class: "hover:text-gray-200" %></li>
        <li><%= link_to 'About', about_path, class: "hover:text-gray-200" %></li>
      </ul>
    </nav>
  </header>

  <div class="container mx-auto px-4 py-8">
    <%= form_with(url: recreation_complexes_path, method: "get", local: true, class: "flex items-center mb-4") do |form| %>
      <div class="flex-1 mr-4">
        <%= form.text_field :search, placeholder: "Search by name...", class: "form-control w-full" %>
      </div>
      <%= form.submit "Search", class: "px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600 focus:outline-none focus:bg-blue-600" %>
    <% end %>
  </div>

  <main class="container mx-auto px-4">
    <%= yield %>
  </main>
</body>
</html>
